(function (window, factory) {
  'use strict';
  if (typeof define === 'function' && define.amd) {
    // AMD
    define([], factory);
  } else if (typeof exports === 'object') {
    // Node.js
    module.exports = factory();
  } else {
    // Browser
    window.<%= exportName %> = factory();
  }
}(this, function factory() {
  // public API
  return $1;
}));